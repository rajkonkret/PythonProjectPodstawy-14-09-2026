Dla osób na poziomie średniozaawansowanym, można zaproponować bardziej złożone zadania z Pythona, które nadal skupiają się na listach, krotkach, i formatowaniu tekstu, ale wymagają bardziej złożonego myślenia i operacji. Oto kilka przykładów:

1. Zadanie: Wyodrębnienie Elementów z Listy List
Mając listę list (na przykład [[1, 2], [3, 4], [5, 6]]), stwórz nową listę, zawierającą tylko drugie elementy każdej wewnętrznej listy.

2. Zadanie: Usuwanie Duplikatów z Listy bez Użycia set()
Usuń duplikaty z listy, ale bez użycia konwersji do zbioru (set()).

3. Zadanie: Znajdowanie Wszystkich Par Liczb Sumujących się do Określonej Wartości
Dla danej listy liczb i wartości docelowej, znajdź wszystkie pary liczb w liście, które sumują się do tej wartości.

4. Zadanie: Sortowanie Listy Krotek
Posortuj listę krotek po drugim elemencie każdej krotki (na przykład [(1, 3), (3, 2), (2, 1)]).

5. Zadanie: Implementacja Własnego Algorytmu Sortowania
Zaimplementuj prosty algorytm sortowania (np. sortowanie bąbelkowe) i użyj go do posortowania listy.

6. Zadanie: Formatowanie Stringa z Wykorzystaniem Metody .format()
Stwórz złożony string używając metody .format() oraz słownika.

7. Zadanie: Wypakowywanie Krotek w Pętli
Mając listę krotek, wypakuj każdą krotkę w pętli i wykonaj operację na jej elementach.

8. Zadanie: Utworzenie Generatora Liczb Fibonacciego
Napisz generator, który wyprowadza kolejne liczby z ciągu Fibonacciego.

9. Zadanie: Przeszukiwanie Listy za Pomocą Wyrażeń Listowych (List Comprehensions)
Stwórz listę kwadratów liczb parzystych z danej listy liczb przy użyciu wyrażeń listowych.

10. Zadanie: Stwórz Funkcję Formatującą Adresy Email
Napisz funkcję, która przyjmuje listę imion i nazwisk, a zwraca sformatowane adresy email (np. Jan Kowalski -> jan.kowalski@example.com).

---------
Przykładowe Rozwiązania
Wyodrębnienie Elementów z Listy List:

python
Copy code
lista_list = [[1, 2], [3, 4], [5, 6]]
nowa_lista = [element[1] for element in lista_list]
Usuwanie Duplikatów z Listy bez Użycia set():

python
Copy code
lista = [1, 2, 2, 3, 4, 4, 5]
bez_duplikatow = []
[bez_duplikatow.append(x) for x in lista if x not in bez_duplikatow]
Znajdowanie Wszystkich Par Liczb:

python
Copy code
lista = [1, 2, 3, 4, 5]
suma = 5
pary = [(x, y) for x in lista for y in lista if x + y == suma]
Sortowanie Listy Krotek:

python
Copy code
lista_krotek = [(1, 3), (3, 2), (2, 1)]
lista_krotek.sort(key=lambda x: x[1])
Implementacja Własnego Algorytmu Sortowania:

python
Copy code
def sortowanie_babelkowe(lista):
    n = len(lista)
    for i in range(n):
        for j in range(0, n-i-1):
            if lista[j] > lista[j+1]:
                lista[j], lista[j+1] = lista[j+1], lista[j]
    return lista
Formatowanie Stringa:

python
Copy code
dane = {"imie": "Jan", "nazwisko": "Kowalski"}
tekst = "{imie} {nazwisko} jest programistą.".format(**dane)
Wypakowywanie Krotek w Pętli:

python
Copy code
lista_krotek = [(1, 2), (3, 4), (5, 6)]
for a, b in lista_krotek:
    print(f"Suma: {a + b}")
Generator Liczb Fibonacciego:

python
Copy code
def gen_fibonacci():
    a, b = 0, 1
    while True:
        yield a
        a, b = b, a + b
Przeszukiwanie Listy za Pomocą Wyrażeń Listowych:

python
Copy code
liczby = [1, 2, 3, 4, 5, 6]
kwadraty_parzystych = [x**2 for x in liczby if x % 2 == 0]
Stwórz Funkcję Formatującą Adresy Email:

python
Copy code
def formatuj_email(imie_nazwisko):
    return f"{imie_nazwisko.lower().replace(' ', '.')}@example.com"
adresy_email = [formatuj_email(osoba) for osoba in ["Jan Kowalski", "Anna Nowak"]]




